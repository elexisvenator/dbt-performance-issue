# add dependencies. these will get pulled during the `dbt deps` process.

packages:
  - git: "https://github.com/fishtown-analytics/dbt-utils.git"
    revision: 0.1.8
  - git: "https://github.com/PageUpPeopleOrg/pageup-dbt-utils.git"
    revision: 1.2.0